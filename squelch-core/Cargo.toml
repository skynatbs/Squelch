[package]
name = "squelch-core"
version = "0.1.0-alpha"
edition = "2024"
publish = false
description = "Shared types, config, error types, and squad model for Squelch."

[dependencies]
thiserror = { workspace = true }
serde = { workspace = true }
